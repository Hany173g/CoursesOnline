<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= course.name %> - CourseOnline</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    .course-img {
      width: 100%;
      max-height: 400px;
      object-fit: cover;
      border-radius: 6px;
    }
  </style>
</head>
<body>

  <%- include('./parts/navbar', { userId: userId }) %>

  <main class="container my-5">
    <div class="row">
      <div class="col-md-6">
        <img src="/<%= course.image %>" alt="<%= course.name %>" class="course-img rounded" />
      </div>
      <div class="col-md-6">
        <h1><%= course.name %></h1>
        
        <p class="text-muted">
          Category: 
          <%= course.Categories 
              ? course.Categories.charAt(0).toUpperCase() + course.Categories.slice(1) 
              : 'No Category' 
          %>
        </p>
        
        <p><%= course.description %></p>
        
        <h4>Instructor: <%= course.instructor || 'Unknown' %></h4>
        
        <a href="/enroll/<%= course._id %>" class="btn btn-primary btn-lg mt-3">Enroll Now</a>
      </div>
    </div>
  </main>

  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
